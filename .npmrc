# Expo/Metro cannot reliably resolve React Native under pnpm's isolated
# node_modules symlink layout. Per ADR-0001 the documented fallback is a
# hoisted node-linker, which also keeps better-sqlite3's native build simple.
node-linker=hoisted
